# Test Auth should check if it already has tokens

# Test Auth should check if it already have a device code

# Test auth saves device code

# Test auth should request a revice code if it doesn't already have one

# Test auth polls for authorization tokens after it has a devie code

# Test auth saves auth tokens when recieved

# Test auth deletes device code once it recieves auth code

# Test saves auth tokens when recieved

# Test auth requests new auth tokens when current ones are about to expire

# Test auth returns headers empty if auth is disabled

# Test auth returns correct heads for auth and team selection