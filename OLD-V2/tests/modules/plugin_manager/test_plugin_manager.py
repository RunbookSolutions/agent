# Test that a plugin is saved to disk

# Test plugin is removed from disk

# Test that a loaded plugin loads its commands

# Test that unloading a plugin unloads its commands

# Test that it can check if a command is loaded based